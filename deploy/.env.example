# Trust & Evidence Sidecar + Adapter environment placeholders

# --- Sidecar auth ---
TRUST_JWT_ISSUER=example-issuer
TRUST_JWT_AUDIENCE=trust-evidence
TRUST_JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----
TRUST_JWT_HS256_SECRET=change-me

# --- Authorization scopes ---
TRUST_SCOPE_GATE_WRITE=trust:gate:write
TRUST_SCOPE_AUDIT_READ=trust:audit:read

# --- Storage ---
TRUST_STORE_BACKEND=filesystem
TRUST_STORE_FILESYSTEM_DIR=.trust_evidence
TRUST_STORE_POSTGRES_DSN=postgresql://user:pass@host:5432/db
TRUST_RETENTION_DAYS=30
TRUST_ENCRYPTION_KEY=

# --- Export / audit ---
TRUST_EVIDENCE_AUDIT_OUTPUT_DIR=.trust_evidence/audit
TRUST_EVIDENCE_MODE=observe

# --- Compatibility/runtime metadata ---
ONYX_GIT_COMMIT=
GIT_COMMIT=
ONYX_VERSION=Development
