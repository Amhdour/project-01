# Alembic-style migrations

v0.1 sidecar uses SQL migration files in `versions/` that can be executed by the sidecar migration runner.
The layout is Alembic-compatible for future cutover to the Alembic CLI.
