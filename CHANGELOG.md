# Changelog

## v0.1.0 (skeleton)
- Added product skeleton docs for Sidecar + Onyx Adapter.
- Added initial workspace layout under `trust-evidence/`.
- Added root `Makefile` with `fmt`, `test`, and `verify` targets.
- Added `deploy/.env.example` placeholder for required env keys.
